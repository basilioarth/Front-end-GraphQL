# Resumo da Aula – Introdução ao Curso (Front-end do MindShare com React + GraphQL)

## Problemas comuns ao consumir APIs tradicionais (REST)
- **Underfetching**: precisar fazer várias requisições HTTP para montar uma única tela.
- **Overfetching**: receber muito mais dados do que o componente realmente vai usar.

## Objetivo do curso
Matar de vez esses problemas utilizando todo o potencial do **GraphQL** no front-end** com **React**.

## O que já temos pronto (do curso anterior)
- Back-end completo do projeto **MindShare** implementado com **GraphQL**.
- Funcionalidades já criadas:
  - Autenticação e cadastro de usuários
  - Fluxo completo de ideias (posts)
  - Comentários
  - Sistema de votos (upvote/downvote)
- API GraphQL já permite fazer **uma única query** para buscar exatamente todos os dados que um componente precisar (adeus múltiplos endpoints REST como `/users`, `/posts`, `/comments` etc.).

## Stack que será usada no front-end
- **React** + **TypeScript** (tipagem e segurança)
- **Vite** (build tool rápida e moderna)
- **Tailwind CSS** + **shadcn/ui** (estilização e componentes prontos)

## Foco prático das aulas
- Integrar React com a API GraphQL já existente
- Buscar **apenas os dados necessários** para cada componente (zero over/underfetching)
- Implementar fluxo completo de autenticação/cadastro no front
- Construir as telas de:
  - Listagem e sugestão de ideias
  - Sistema de votos
  - Comentários
- Tudo isso com **consultas GraphQL únicas e otimizadas**

## Benefício principal do GraphQL para o front-end
Permite criar interfaces ricas e performáticas sem precisar ficar fazendo malabarismos com várias requisições HTTP simultâneas.

Próximos passos: configurar o projeto React com Vite e começar a consumir as primeiras queries GraphQL.

Bora pro código! 🚀